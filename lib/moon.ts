// Wrapper um astronomy-engine für alle Mondberechnungen – Implementierung folgt in Etappe 2 (SPEC.md §3).

export function getNextFullMoon(from: Date = new Date()): Date | null {
  return null;
}

export function getPreviousFullMoon(from: Date = new Date()): Date | null {
  return null;
}

export function getFullMoonsInRange(start: Date, end: Date): Date[] {
  return [];
}

export function getCurrentIllumination(at: Date = new Date()): number {
  return 0;
}
